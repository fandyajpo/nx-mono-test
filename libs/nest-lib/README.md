# nest-lib

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build nest-lib` to build the library.

## Running unit tests

Run `nx test nest-lib` to execute the unit tests via [Jest](https://jestjs.io).
